from django.apps import AppConfig


class PortfolioConfig(AppConfig):
    """
    Class that configures the Portfolio
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'portfolio'
